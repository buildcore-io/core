import { WEN_FUNC } from '@build-5/interfaces';
import { https } from '../../..';

export const createCollection = https[WEN_FUNC.createCollection];
export const updateCollection = https[WEN_FUNC.updateCollection];
export const rejectCollection = https[WEN_FUNC.rejectCollection];
export const mintCollection = https[WEN_FUNC.mintCollection];
